# 方案设计（你论文里可以直接写的那种）

## Step 0：输入与目标

* 输入：VAE 得到的隐空间表示 $Z\in\mathbb{R}^{n\times d} $，硬标签 $y\in{0,1}^n$（二分类且不平衡）。
* 目标：

  1. 通过图传播得到软标签 $p_i\in[0,1]$（边界点更“软”、簇内部更“硬”）
  2. 用代价敏感损失训练分类器，使 **少数类被预测低分（FN）** 遭受更大惩罚
  3. 在验证集上自动寻找最优阈值（而不是固定 0.5）

---

## Step 1：在隐空间构图（kNN 图 + RBF 权重）

对每个样本 $i$，取其 $k$ 个近邻 $\mathcal{N}(i)$，并用 RBF 核得到节点$i$和节点$j$的边权重为权重：
$$
w_{ij}=\exp\left(-\frac{\|z_i-z_j\|^2}{2\sigma_i^2}\right),\quad j\in\mathcal{N}(i)
$$
其中 $\sigma_i$ 用局部自适应带宽（邻域平均距离）：
$$
\sigma_i=\text{mean}_{j\in\mathcal{N}(i)} \|z_i-z_j\|
$$

进一步，为了使少数类（正样本）更具有影响力，给邻接权重$w_{ij}$ 乘一个“邻居类别系数”：
$$
\tilde{w}_{ij}=w_{ij}\cdot\eta^{\mathbf{1}[y_{j}=1]}
$$
其中，$\eta$是超参数，取值范围为1~3，会根据数据的不平衡比进行调整。不平衡程度约搞，$\eta$的值越大。（当前还是手动设置，暂时没有设置计算公式）

然后，构造标签传播算法的随机游走矩阵（概率转移矩阵）（行归一化）：
$$
S_{ij}=\frac{\tilde{w}_{ij}}{\sum_{j\in\mathcal{N}(i)} \tilde{w}_{ij}}
$$

---

## Step 2：只用“高置信核心点”当 seeds，再做 Label Propagation

如果所有点都当作已标注点，传播会退化为“没传播”。所以必须挑选 seeds：

* 定义邻域纯度（purity）：
  $$
  \text{purity}(i)=\frac{1}{k}\sum_{j\in\mathcal{N}(i)}\mathbf{1}[y_j=y_i]
  $$
* 对每一类分别选 purity 最高的前 `seed_ratio` 作为 seeds。

设 $F_i=[P(y=0|z_i),P(y=1|z_i)]\in\mathbb{R}^2$，传播迭代：
$$
F_i^{(t+1)}=\alpha_i\sum_jS_{ij}F_j^{(t)}+(1-\alpha_i)Y_i
$$
其中：

$$\alpha_i=\begin{cases}\alpha^+&y_i=1\\\alpha^-&y_i=0\end{cases},\quad\alpha^+=0.80,\:\alpha^-=0.95$$
* 关于 $\alpha_i$，基于少数类样本更大影响力：
  * 正类（少数类）：$\alpha_i$小一点，（更依赖原始 $Y_i$ ，更不容易被邻居拉走）
  * 负类（多数类）：$\alpha_i$大一点，（更依赖邻居扩散，更容易被正类信号拉动）
* $Y_i$ 为原始硬标签
* 每轮迭代对 seeds 做 clamp：强制回到 one-hot 防漂移

得到软标签：
$$
p_i = F_{i,1}
$$

--- 

## Step 3：代价敏感训练（少数类低分→更大惩罚）
使用一个MLP网络学习数据$Z$ 以及它的软标签。该MLP的损失函数如下：


用 `BCEWithLogits` 的 `pos_weight` 做最稳定的“FN 惩罚增强”：
$$
\text{pos\_{weight}}=\frac{N_{neg}}{N_{pos}}
$$

并且用样本级权重强调边界点（可选但很契合你的动机）：
$$
w_i = 1+\lambda\cdot (1-|2p_i-1|)
$$
越接近 0.5 权重越大。

最终损失：
$$
\mathcal{L}=\frac{1}{n}\sum_i w_i\cdot \text{BCEWithLogits}(logit_i, p_i;\ \text{pos\_weight})
$$